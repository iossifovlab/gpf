/wdae-logs
# Written by web_e2e/Jenkinsfile.e2e's `Compose env` stage: the explicit
# AGENT_UID / AGENT_GID / GRR_ROOT that every `docker compose` call
# is pointed at with --env-file. Per-agent build state, never committed.
/.env
