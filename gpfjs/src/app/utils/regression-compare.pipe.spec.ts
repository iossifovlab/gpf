import { RegressionComparePipe } from './regression-compare.pipe';

describe('ComparePipe', () => {
  it('should create an instance', () => {
    const pipe = new RegressionComparePipe();
    expect(pipe).toBeTruthy();
  });
});
