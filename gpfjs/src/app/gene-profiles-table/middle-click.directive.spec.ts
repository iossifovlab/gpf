import { MiddleClickDirective } from './middle-click.directive';

describe('MiddleClickDirective', () => {
  it('should create an instance', () => {
    const directive = new MiddleClickDirective();
    expect(directive).toBeTruthy();
  });
});
