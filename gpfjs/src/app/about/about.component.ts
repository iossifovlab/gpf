import { Component } from '@angular/core';

@Component({
  selector: 'gpf-about',
  templateUrl: './about.component.html',
  styleUrls: ['./about.component.css']
})
export class AboutComponent {
  public write(markdown: string): void {
    // to do
  }
}
