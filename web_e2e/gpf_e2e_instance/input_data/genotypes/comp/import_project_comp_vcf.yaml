id: comp_vcf_liftover

input:
  input_dir: "%(wd)s"
  pedigree:
    file: comp.ped

  vcf:
    files:
      - comp_liftover.vcf

processing_config:
  work_dir: "%(wd)s/OUTPUT"

destination:
  storage_id: genotype_duckdb