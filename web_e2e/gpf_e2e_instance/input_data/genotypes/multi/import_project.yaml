id: multi_liftover

input:
  input_dir: "%(wd)s"
  pedigree:
    file: multi.ped
  vcf:
    files:
      - multi.vcf

processing_config:
  work_dir: "%(wd)s/OUTPUT"

destination:
  storage_id: genotype_duckdb
