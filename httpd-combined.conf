## Apache config for the combined gpf-web image.
##
## Adapted from web_ui/httpd.conf for Debian's apache2
## layout (modules at /usr/lib/apache2/modules/, run dirs
## under /var/{run,log}/apache2). The reverse-proxy target
## is 127.0.0.1:9001 because gunicorn runs in the same
## container under supervisord (instead of a separate
## "backend" service like in the docker-compose split-image
## deployment).

ServerRoot "/etc/apache2"
ServerName localhost
Listen 80
User ${APACHE_RUN_USER}
Group ${APACHE_RUN_GROUP}
PidFile ${APACHE_PID_FILE}

LoadModule mpm_event_module       /usr/lib/apache2/modules/mod_mpm_event.so
# Modules NOT loaded — built into Debian's apache2 binary
# (verified with `apache2 -l`: core, mod_so, mod_watchdog,
# http_core, mod_log_config, mod_logio, mod_version,
# mod_unixd). LoadModule for any of these aborts startup
# with "module X is built-in and can't be loaded". The MPM
# module above is a separate .so on Debian, so it stays.
#   built-in: log_config_module
#   built-in: unixd_module
LoadModule authz_core_module      /usr/lib/apache2/modules/mod_authz_core.so
LoadModule mime_module            /usr/lib/apache2/modules/mod_mime.so
LoadModule dir_module             /usr/lib/apache2/modules/mod_dir.so
LoadModule alias_module           /usr/lib/apache2/modules/mod_alias.so
LoadModule headers_module         /usr/lib/apache2/modules/mod_headers.so
LoadModule filter_module          /usr/lib/apache2/modules/mod_filter.so
LoadModule deflate_module         /usr/lib/apache2/modules/mod_deflate.so
LoadModule expires_module         /usr/lib/apache2/modules/mod_expires.so
LoadModule rewrite_module         /usr/lib/apache2/modules/mod_rewrite.so
LoadModule remoteip_module        /usr/lib/apache2/modules/mod_remoteip.so
LoadModule proxy_module           /usr/lib/apache2/modules/mod_proxy.so
LoadModule proxy_http_module      /usr/lib/apache2/modules/mod_proxy_http.so
LoadModule proxy_wstunnel_module  /usr/lib/apache2/modules/mod_proxy_wstunnel.so
LoadModule setenvif_module        /usr/lib/apache2/modules/mod_setenvif.so

ErrorLog  /proc/self/fd/2
CustomLog /proc/self/fd/1 common
LogLevel  warn

TypesConfig /etc/mime.types

DocumentRoot "/var/www/html"
<Directory "/var/www/html">
    Options FollowSymLinks
    AllowOverride None
    Require all granted
    DirectoryIndex index.html

    # SPA fallback: anything that isn't a real file or
    # directory under /, and isn't headed for /api/, /ws/,
    # /o/, /accounts/, or /static/, serves index.html so
    # client-side routing works. /o/ and /accounts/ are part
    # of the OAuth login round-trip — the SPA's log-in button
    # navigates to /o/authorize/?..., Django redirects to
    # /accounts/login/, and the form post returns through
    # both. Excluding them here mirrors web_ui/httpd.conf so
    # the rewrite never swallows the auth flow into index.html.
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} !^/api/
    RewriteCond %{REQUEST_URI} !^/ws/
    RewriteCond %{REQUEST_URI} !^/o/
    RewriteCond %{REQUEST_URI} !^/accounts/
    RewriteCond %{REQUEST_URI} !^/static/
    RewriteRule ^ /index.html [L]
</Directory>

# Django admin / DRF static — baked into the image at build
# time by collectstatic in the upstream gpf-web-ui image,
# carried over via the COPY in Dockerfile.production.
Alias /static/ "/var/www/html/static/"
<Directory "/var/www/html/static/">
    Options FollowSymLinks
    AllowOverride None
    Require all granted
    ExpiresActive On
    ExpiresDefault "access plus 1 year"
    Header set Cache-Control "public, immutable"
</Directory>

# Long cache on hashed Angular assets, no-cache on
# index.html so a deploy is picked up immediately.
<Files "index.html">
    Header set Cache-Control "no-store, no-cache, must-revalidate"
</Files>
<FilesMatch "\.(js|css|woff2?|svg|png|jpg|jpeg|gif|ico)$">
    ExpiresActive On
    ExpiresDefault "access plus 1 year"
    Header set Cache-Control "public, immutable"
</FilesMatch>

# Reverse-proxy /api/, /o/, /accounts/ to the in-container
# gunicorn. /o/ is the OAuth2 endpoint (django-oauth-toolkit)
# that the SPA's log-in button hits; /accounts/ is Django's
# auth views that /o/authorize redirects through (LOGIN_URL
# setting). Mirrors web_ui/httpd.conf so the OAuth round-trip
# resolves the same way in either deployment shape.
ProxyPreserveHost On
ProxyRequests Off
RequestHeader set X-Forwarded-Proto "%{REQUEST_SCHEME}e"
ProxyPass        /api/      http://127.0.0.1:9001/api/
ProxyPassReverse /api/      http://127.0.0.1:9001/api/
ProxyPass        /o/        http://127.0.0.1:9001/o/
ProxyPassReverse /o/        http://127.0.0.1:9001/o/
ProxyPass        /accounts/ http://127.0.0.1:9001/accounts/
ProxyPassReverse /accounts/ http://127.0.0.1:9001/accounts/

# WebSockets via mod_proxy_wstunnel.
ProxyPass        /ws/ ws://127.0.0.1:9001/ws/
ProxyPassReverse /ws/ ws://127.0.0.1:9001/ws/

# Compression for text types.
AddOutputFilterByType DEFLATE \
    text/plain text/html text/css text/xml \
    application/javascript application/json image/svg+xml
