[study]

prefix = quads_in_child
type = vcf
phenotypes = autism

name = QUADS_IN_CHILD
id = quads_in_child

description = QUADS IN CHILD

data_dir = %(work_dir)s/

phenotypeGenotypeTool = yes
phenotypeBrowser=no

enrichmentTool = no

genotypeBrowser=no
