[dataset]

dataset_name = QUADS_IN_CHILD
dataset_id = quads_in_child

description = QUADS IN CHILD

data_dir = %(work_dir)s/

studies = quads_in_child
# variants_prefix = %(data_dir)s/quads_in_child

phenoGenoTool = yes
phenotypeBrowser=no

enrichmentTool = no

genotypeBrowser=no
