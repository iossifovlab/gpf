[dataset]

dataset_name = QUADS_IN_CHILD
dataset_id = quads_in_child

description = QUADS IN CHILD

data_dir = %(work_dir)s/

study_group = quads_in_child

phenotypeTool = yes
phenotypeBrowser=no

enrichmentTool = no

genotypeBrowser=no
