[general]
header = chr, pos(1-coor), ref, alt, score, score2
noScoreValue = na

[columns]
chr = chr
pos_begin = pos(1-coor)
pos_end = pos(1-coor)
ref = ref
alt = alt
score = score, score2

[schema]
str = chr, ref, alt
int = pos(1-coor)
float = score, score2

[misc]
chr_prefix = False
pos_base = 1
format = bedGraph
compression = gzip
